WORKFLOW: /task-template

When the user provides a task, enforce this input template.
If any section is missing, ask ONLY for the missing parts.

USER INPUT TEMPLATE:

GOAL:
CURRENT BEHAVIOR:
EXPECTED BEHAVIOR:
CONSTRAINTS (do-not-touch / locked modules / single-source rules):
FILES / LOGS (paths, errors, stack traces, screenshots):
REPRO STEPS (1-5 steps, if bug):
DONE CRITERIA (how we know it’s finished):